# ── pip global flags ─────────────────────────────────────────────────────────
# --prefer-binary       → prefer pre-built wheels when available; still allows
#                         pure-Python source installs (langdetect has no wheel).
# --only-binary targets → force wheels specifically for heavy C/Fortran libs
#                         (scipy, numpy, torch) that fail to compile from source
#                         on python:3.10-slim and Render (no gfortran toolchain).
--prefer-binary
--only-binary=scipy,numpy,torch,scikit-learn,pandas,pyarrow,pillow

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
httpx==0.27.0
python-dotenv==1.0.0

# ── Language detection ────────────────────────────────────────────────────────
langdetect==1.0.9

# ── HuggingFace stack  (translation + TTS — replaces deep-translator & gtts) ──
# NLLB-200 (translation) + SpeechT5 / MMS-TTS (TTS) live here
transformers>=4.40.0,<5.0.0   # v5 removed the "translation" pipeline task
torch>=2.1.0
sentencepiece>=0.1.99         # tokeniser required by NLLB-200
protobuf>=3.20.0              # dependency of sentencepiece
accelerate>=0.27.0            # speeds up model loading on CPU
datasets>=2.14.0              # HuggingFace datasets (no loading-script datasets used)
pandas>=2.0.0                 # read parquet files from HF dataset repos
pyarrow>=14.0.0               # parquet backend for pandas
soundfile>=0.12.1             # WAV I/O for SpeechT5 / MMS-TTS output
sentence-transformers>=2.2.2  # semantic hotel re-ranking (all-MiniLM-L6-v2)

# ── STT  (Whisper — unchanged) ────────────────────────────────────────────────
faster-whisper>=1.0.3     # pin floor only; exact pin can fail on Render's pip mirror
ctranslate2>=4.6.1

# ── Audio processing ──────────────────────────────────────────────────────────
pydub==0.25.1
numpy>=1.24.0             # 1.26.4 not indexed on Render; allow any compatible build
noisereduce==3.0.2
scipy>=1.13.0             # 1.13.0 not indexed on Render; allow any compatible build

# ── Storage ───────────────────────────────────────────────────────────────────
redis==5.0.1              # standard redis-py (TCP + rediss:// TLS for Upstash)
upstash-redis>=1.1.0      # Upstash HTTP REST client for HF Spaces (no TCP needed)

# ── Database ──────────────────────────────────────────────────────────────────
# Direct PostgreSQL with DATABASE_URL — no Supabase SDK needed.
psycopg2-binary>=2.9.9    # sync driver for processor.py (booking state machine)
asyncpg>=0.29.0            # async driver for hf_space/db/supabase.py (routers)